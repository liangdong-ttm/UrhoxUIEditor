# 持续验收进度

本清单供当前任务后续续跑使用。不是待实现功能愿望清单；每轮只完成一个可验证的工作流，
保留其它任务和用户已有修改，不要求把所有功能一次性重做。

## 工作边界

- 编辑器目录：`/Users/liangdong/Documents/MakerTools/UrhoxUIEditor`。
- 真实游戏：`/Users/liangdong/Documents/MakerGame/奥德彪拉香蕉/BikeKingBanana`，只读。
- 预览：`http://127.0.0.1:4191/?project=local`；服务已在后台运行，不应重复启动。
- 不自行 commit、push、部署、授权云服务或改游戏文件。
- 本次安排每小时续跑，最多 8 次；北京时间 2026-09-22 10:00 前收束并总结。
- 本机和 Codex 必须可运行才能执行续跑；不得把计划执行写成已经完成。
- 前一轮真实 UI 渲染验收详见 `docs/bikekingbanana-acceptance.md`。

## 本轮完成：保存与项目切换

### 已复现并修复

1. 本地文件句柄权限被拒绝时，原实现仍可能向编辑器服务器保存并返回成功。
   现在句柄是唯一目标；失败直接返回错误，不更换写入目的地。
2. 写入异常可能逃逸并遗留打开的写入流。现在返回明确失败，并尝试 abort 释放流。
3. 保存过程中继续编辑，新修改曾被成功回调错误标成已保存。
   现在在任何异步等待前冻结目标和 JSON，成功后以真正写入的快照更新 baseline。
4. 重复保存曾并发写同一个文件。现在共享进行中的保存任务，并显示“保存中…”。
5. 切换项目在确认离开之前就改写当前项目索引、权限和名称。
   现在先建立独立候选索引，确认离开并加载成功后再安装为当前项目。
6. 取消选择、拒绝授权、选择空目录、加载失败不得破坏原来的项目状态。
7. 文档读取失败会显示文件名和错误；新文档成功加载前不清除旧文档 dirty。
8. 文档切换期间串行处理，禁用工作区输入并显示状态，防止慢响应互相覆盖。
9. JSON 根节点/children 结构先校验；预览先在临时副本中展开和布局，再替换活动文档。
10. 初始示例和清单的迟到响应不能覆盖用户已打开的项目。
11. 浏览器实测发现：属性输入失焦会无条件重建底部列表，第一次点击文件被吞掉。
    现在 dirty 没有变化时不重建文件列表。

### 自动化证据

- `tests/save-regression.test.js`：权限拒绝不改写其它目标、写失败 abort、正常句柄写入、
  显式服务器写入路径仍可用。
- `tests/project-session.test.js`：保存中新增修改、重复保存、取消项目切换、空目录、
  坏 JSON 读取失败保留 dirty、慢文件打开串行、dirty 不变时不销毁列表。
- `tests/p0-editor.test.js`：增加非法节点声明的结构校验。
- 新测试均并入 `node tests/run-all.js`。测试使用隔离 VM、内存文件句柄和延迟 Promise，
  不写真实游戏工程。

### 浏览器证据

在真实项目签到页实际完成：

1. 选中领取按钮，输入“续跑验收 临时文字”。
2. Tab 离开输入框，第一次点击设置文件即可弹出未保存确认。
3. 点击取消，保留签到页与临时文字、dirty。
4. 一次撤销恢复原文，dirty 消失。
5. 再次点击设置文件可正常切换，没有界面锁死。
6. 页面控制台本轮未观察到 error/warn。

临时编辑已撤销，预览停留在设置页，无未保存修改。
真实系统目录授权和写回未通过浏览器执行，不能把模拟句柄测试写成系统授权实测。

## 续跑 1：快捷键与模态确认（2026-09-22 02:35）

### 已复现并修复

- 浏览器复现未保存确认框 Escape 无效，焦点仍留在后台；此前弹窗只是普通遮罩。
  保存确认、删除确认、缺少 UI 提示改用原生 `dialog.showModal()`，
  用浏览器原生模态隔离后台点击和焦点，标注可访问标题。
- 保存/删除默认聚焦取消。保存确认 Escape 与取消按钮走同一条离开取消链路，
  保留 dirty 并释放 opening 锁；删除确认 Escape 清除待删除目标。
- 输入框内 Cmd/Ctrl+S 不再被 isTyping 提前吞掉。忙碌/模态期间不执行保存，
  但仍阻止浏览器“保存网页”；普通文本撤销、退格继续属于输入框。
- 模态期间不响应编辑器撤销、移动、删除、层级等快捷键；Space 不再抢占
  对话框及按钮的原生激活。已处理事件和输入法组合输入不触发编辑命令。
- Shift+0/1/2 改为识别物理 Digit 键，兼容实际 key 为 `)`、`!`、`@` 的情况。
  不截获额外带 Cmd/Ctrl/Alt 的系统组合键。
- 对话框限制到视口内，长文件路径可换行；未引入新的依赖或通用弹窗框架。

### 验证证据

- 新增 `tests/hotkeys-regression.test.js`，先得到输入框保存未 preventDefault 的失败，
  修复后验证输入焦点、模态隔离、忙碌状态、组合输入、已处理事件和 Shift 数字。
- `tests/project-session.test.js` 增加原生 cancel 事件回归：
  取消待切换项目后原项目对象/dirty 不变，对话框关闭且 opening 锁释放。
- 真实浏览器：设置标题临时编辑 → 切换签到页 → 保存确认聚焦取消 →
  Cmd+Z/方向键未影响后台标题 → Tab 到不保存/保存 →
  Escape 取消仍保留设置页和临时文字。
- 在 Text 输入框按 Cmd+S，出现编辑器已有只读权限提示，未写入游戏。
  随后离开输入框，用编辑器撤销恢复原文，dirty 消失。
- 打开删除确认但不执行删除；方向键不改节点，Escape 关闭并恢复
  `treeDeleteBtn` 焦点。Shift+0 实测变为 100%，Shift+1 恢复 47%。
- `node tests/run-all.js` 全部通过；`python3 -B tests/test_serve.py` 4 项通过；
  真实项目 37 文件/392 节点、缺图 0、序列化回环通过；`git diff --check` 通过。
  浏览器控制台未观察到 error/warn。

### 保留的限制与交接

- 没有通过 GUI 执行确认删除、真实目录授权或真实文件写回；不能把模拟测试说成系统实测。
- 缺少 UI 提示框已使用同一原生模态机制，但本轮未通过真实目录选择独立验收。
- 保存确认的正向 Tab 路径已验收；Shift+Tab 可能进入浏览器自身控件，
  不会进入后台编辑器，不增加自制焦点循环。
- 隐藏 `folderInput` 导入旧链路仍未修复；后续检查其是否有实际可达入口，
  再决定接入现有候选项目事务或移除死入口。
- 预览停留在设置页、无未保存修改、适应画布；现有服务保留，无提交/推送/部署。
- 下一轮优先 Assets 完整流程：异步加载隔离、缺图结束、URL 回收及替换对象/取消。

## 续跑 2：Assets 加载与图片替换（2026-09-22 03:33）

### 已复现并修复

- 延迟文件读取在切换项目后仍生成 URL 并写入新缓存。增加资源上下文代次，
  旧代读取结果和图片完成事件不再回写新上下文或触发新文档重绘。
- 同一缩略图先绑定 A、再绑定 B 时，A 的迟到结果可能盖住 B。
  每个 img 的绑定有独立身份，清空绑定立即移除旧 src。
- blobMap 的 File 被挂上 `_url` 且未统一回收；底部缩略图又维护第二套 `_url`。
  现在画布、Inspector、Assets 缩略图统一经资源模块解析，创建的对象 URL
  都由上下文持有并在切换时回收，不修改 File 对象。
- 找不到本地图片句柄或读取权限异常时，之前不设置 src，也不结束加载。
  现在失败同样完成一次通知；缓存中的所有等待者都能收到完成通知，
  同一个重绘回调不会重复排队，已加载图片不会递归触发重绘。
- 浏览器复现选图期间切换节点、Escape 都没有取消替换状态。
  现在替换目标绑定项目、源文档身份和唯一选中节点；切换对象/文档、
  撤销导致源树替换或多选后失效。执行前再次核对，防止写到旧对象。
- 新增“取消选图”，Escape 优先退出选图并保留当前节点；
  状态栏显示正在替换的目标。返回 UI 文档也取消选图。
- 替换背景图曾强制写 `role = "Image"`，会破坏按钮等控件身份。
  现在只修改请求的图片字段；选择原图不生成无效历史事务。

### 验证证据

- 新增 `tests/assets-regression.test.js`，先复现旧上下文 URL 污染失败，再验证：
  跨项目迟到读取、同元素重复绑定、File 对象复用及 URL 回收、缺句柄、
  同步抛出权限错误、多等待者、过期图片完成事件。
- `tests/project-session.test.js` 先复现替换导致角色变为 Image，
  再验证角色保留、修改前后事务顺序、原图无变化、选中对象变化、
  显式取消和源文档身份变化后的替换拒绝。
- `tests/hotkeys-regression.test.js` 增加 Escape 先取消选图、再次才取消节点选择。
- 真实浏览器：settings-back 开始换图 → 切换 settings-title 自动取消；
  再次换图 → Escape 取消；显式“取消选图”也正常，无 dirty。
- 将返回按钮换成 `reward_coin.png`，画布和 Inspector 都显示金币，
  组件仍显示 Button 且按钮属性仍在；一次撤销恢复 `back_button.png`，
  dirty 消失。
- Assets 中金币拖到画布，节点数 30 → 31，未重复创建；
  一次撤销恢复 30，dirty 消失。这补齐了之前 drop 冒泡修复的真实浏览器证据。
- 本轮浏览器检查 Assets 401 个缩略图，全部 complete 且 naturalWidth 非零，
  控制台未观察到 error/warn。
- 全部 JS 测试通过，Python 4 项通过，37 文件/392 节点结构与序列化回环通过，
  `git diff --check` 通过。

### 保留的限制与交接

- 跨项目本地句柄、权限错误和 URL 回收通过隔离 VM/内存句柄验证，
  未通过 GUI 授权真实目录；不写入真实游戏文件。
- 拖入画布已实测；拖入 Inspector 指定字段尚未单独浏览器验收。
- 资源搜索、目录导航效率、缺图可见诊断、TGA 浏览器解码支持仍属后续信息架构/
  兼容性验收，不把本轮记作所有 Assets 产品细节完成。
- 最终停留设置页、UI 文档栏、47% 适应画布，无未保存修改，原服务保留。
- 下一轮优先节点树完整工作流，重点折叠、隐藏/锁定继承、改父、重排和撤销。

## 续跑 3：节点树导航、排序与层级保护（2026-09-22 04:37）

### 已修复

- 改父命令补上源节点/目标及祖先锁定、根节点、脱离活动树对象、
  生成节点和循环层级保护。测试先复现了锁定父节点的子节点仍被拖走。
- 树拖拽区分上 1/4 插到前面、中间 1/2 作为子节点、下 1/4 插到后面，
  显示插入线或容器描边，支持同级重排；无变化不增加历史。
  使用专用节点拖拽类型，外部图片/文字不作为树节点改父。
- 流布局节点改父保留百分比尺寸和定位声明，由新父容器布局决定位置；
  绝对定位改父保留世界矩形，并用实际布局校正目标父节点边框原点。
  后者会把响应尺寸固化为改父前的像素尺寸，这是保留当前视觉矩形的取舍，
  不宣称同时保留跨容器百分比语义。
- 行不再使用嵌套 HTML button，改为 tree/treeitem，暴露层级、选中及展开状态。
  折叠按钮有名称；树行方向键导航/展开/折叠，Enter/Space 选择，
  选中和折叠重绘后恢复树行焦点，避免方向键误移画布节点。
- 树中显示祖先隐藏和锁定状态；锁定后代禁止拖动，根节点不提供行内删除按钮。

### 验证证据

- `tests/tree-commands.test.js` 使用真实 Yoga：祖先锁定、同级重排/no-op、
  流布局百分比保留、含 padding/border 的绝对定位改父世界矩形、
  根保护、循环和锁定目标。均通过。
- `tests/tree-interaction.test.js` 使用隔离 DOM：树语义、折叠、焦点、
  上下键不触发画布移动、继承隐藏/锁定、上/中/下落点和外部拖入拒绝。
- 浏览器：settings-panel 折叠时行数 30 → 5，Right 展开到 30，无 dirty；
  选中 settings-back 后 Down 选中 settings-panel，无 dirty。
- 浏览器：settings-back 拖到 settings-background 上沿，两个同级节点顺序交换；
  一次 Cmd+Z 恢复原顺序，dirty 消失。
- 浏览器：隐藏 settings-panel 后 26 行标记隐藏；撤销恢复 0 行隐藏。
  锁定 settings-panel 后 25 个后代提示父级锁定，标题行 draggable=false；
  撤销锁定后恢复干净状态。
- 全部 JS、Python 4 项、37 文件/392 节点项目结构/序列化回环、
  `git diff --check` 通过。

### 未通过项，下一轮优先处理

- **浏览器跨父容器拖拽尚未通过**：settings-title 拖到 settingsContent 行中央
  没有发生层级变更，仍为 level=4 且无 dirty。命令层和模拟落点测试通过，
  但不能替代这条真实交互；需继续定位拖拽事件实际落点/传递，不猜测根因。
- 重命名、树多选范围、锁定后删除/其它命令的一致性尚未完整验收。
- 目前每行可 Tab 聚焦，尚不是完整的 roving-tabindex 树导航实现。
- 临时排序/显隐/锁定改动均已撤销；设置页仍可打开，游戏文件未写入。
- 后续浏览器定位树行应使用 `getByRole('treeitem', ...)`，
  展开按钮为“折叠/展开 节点名”，不再使用旧的整行 button 定位器。

## 续跑 4：快速跨父拖拽闭环（2026-09-22 05:32）

### 根因与修复

- 针对续跑 3 的未通过项，临时记录实际浏览器 dragstart/dragenter/dragover/drop。
  同一路径中源节点成功开始拖拽，沿途多行收到 dragenter，但目标行在松手前
  未收到下一次 dragover，因而没有被接受为落点，也没有收到 drop。
- 将合法落点检查和反馈提取为同一函数，同时绑定 dragenter 与 dragover。
  不更改改父命令规则，不绕过根节点、循环、锁定或生成节点保护。
  所有临时 console 诊断已移除。
- 新增回归断言先复现 dragenter 未被接受，再验证快速进入有效落点时可接收，
  锁定/循环等无效目标仍不接收。

### 浏览器与测试证据

- 真实设置页，settings-title 快速拖到 settingsContent 行中央，
  树层级从 4 变为 3，dirty 正常出现。
- Inspector 相对坐标变为 X=124、Y=216，Width=472、Height=127；
  截图确认标题仍在原有画布位置，没有因改父跳动。
- 一次撤销恢复层级 4 且 dirty 消失；重做回到层级 3；再次撤销恢复干净状态。
  上一轮“浏览器中央改父未通过”在此关闭，不能据此扩大为所有复杂改父均通过。
- 全部 JS 回归、Python 4 项、37 文件/392 节点项目检查、序列化回环和
  `git diff --check` 通过。真实游戏文件未写入，无提交/推送/部署。

### 后续

- 下一轮优先多选选择及 Inspector 的一致性：全选、加减选、
  多选属性是否误导、锁定节点混入选择集合时的操作边界。
- 复杂变换、跨容器百分比语义、自动布局缩放仍保留在各自专项验收中。
- 树重命名与范围多选尚未完整验收，不标记整棵树所有交互完成。

## 续跑 5：多选公共属性与全选（2026-09-22 06:35）

### 已修复

- 多选时 Inspector 不再显示主节点的完整单选面板。明确显示数量和对象列表，
  只展示已有清晰批量语义的公共属性：启用、透明度，以及全部为文字控件时的 Text。
- 公共值一致则显示值，不一致显示“混合”；启用使用三态 checkbox。
  查看混合值不修改任何源属性。
- 批量修改先开始一个事务，再更新所有选中节点，最后统一提交；
  一次撤销恢复每个节点各自的原值，不把主节点旧值覆盖给其它节点。
- 透明度限定有限数值 0 到 1，非法值不部分修改节点；
  清空透明度移除显式属性，恢复默认值。Escape 撤回未提交输入，
  后续失焦不会误把混合文本清空。
- 包含锁定/锁定后代或生成节点时批量字段禁用，不默默只修改集合的一部分；
  提交前核对当前源文档和选中集合，失效输入不改旧对象。
- Cmd/Ctrl+A 不再只是 preventDefault：选择源树所有可编辑非根节点，
  排除隐藏、锁定、生成节点及其子树；输入框内仍保留原生全选。

### 证据

- 测试先复现多选仍伪装主节点，再验证混合值、非法透明度无副作用、
  两节点批量修改与一次撤销、三态启用、批量 Text、Escape 撤回和锁定集合拒绝。
- Doc 与快捷键测试验证全选过滤规则及输入框原生全选。
- 真实浏览器 Shift 选中 settings-title 和 settings-back，
  Inspector 显示“已选择 2 个节点”、Text 混合、透明度 1。
- 原生键盘将透明度改为 0.4 后 Tab 提交，截图确认标题和返回按钮都变透明。
  一次 Cmd+Z 恢复原状态且 dirty 消失。
- Cmd+A 实际选中 29 个非根节点，公共面板显示正确数量；
  不出现主选中节点的名称、位置等单选编辑框。
- 全部 JS、Python 4 项、37 文件/392 节点结构和序列化回环、
  `git diff --check` 通过，游戏文件未修改。

### 限制与下一轮

- 这不是全属性批量编辑：位置/尺寸/图片/主题等仍需各自的共同值及约束语义设计。
- 全选包含父子节点；显式透明度设置应用于每个选中节点，
  渲染时祖先透明度仍按引擎规则累乘，不等同于整个选区的合成透明度。
- 多选复制、删除、编组等命令尚未全部统一；不能把公共属性通过当作所有多选命令通过。
- 下一轮优先移动/缩放约束及手势取消，之后集中处理多选命令和最终回归。

## 续跑 6：画布手势取消与事务收尾（2026-09-22 07:35）

### 已修复

- 先复现 pointercancel 被当作正常完成：Alt 拖拽的临时副本被保留并提交历史。
  现在仅 pointerup 提交；pointercancel、未正常松手的 lostpointercapture、
  Escape 和窗口失焦均回滚本次手势。
- 取消移动/缩放/Alt 复制恢复事务前源树及完整多选集合，不新增撤销步骤，
  不清空之前的重做历史。取消框选恢复框选前的选中集合。
- 结束时先清除 drag，再释放指针捕获，避免 release 引发的 lostpointercapture
  二次提交或把正常完成误当取消；同时清理参考线、框选框及拖动光标。
- 多选拖动过滤锁定节点、锁定后代、根节点及生成节点，
  不再因为拖动其它可编辑节点而带动锁定对象。
- 平移在 pointercancel/lostpointercapture/失焦时清理 panning；
  失焦也清理 Space 状态，避免回到窗口后仍处于空格平移模式。

### 验证证据

- 新增 `tests/gesture-regression.test.js`，执行真实 input.js 事件处理代码，
  使用隔离画布和内存文档，不写真实工程。
- 覆盖 Alt 副本的四类取消、缩放 Escape 回滚、框选取消恢复多选、
  取消保留 redo、捕获释放、锁定节点不跟随移动、正常结束只产生一条历史。
- 真实浏览器拖动 settings-back 右下缩放手柄：
  Width/Height 从 108/108 变为 148/139，X/Y 仍为 26/26；
  一次撤销恢复，dirty 消失。
- 全部 JS、Python 4 项、37 文件/392 节点结构/序列化回环及
  `git diff --check` 通过。

### 未验证范围

- 当前浏览器操作接口的拖拽是完整手势，未在真实拖拽中途注入系统取消或 Escape；
  这些中断路径是事件级回归证据，不称为浏览器端实测。
- Flex resize、百分比/双边约束 resize、复杂多选缩放仍未完整解决，
  不能用此次普通绝对定位按钮缩放代替。
- 下一轮优先多选复制/删除/编组等命令一致性；随后做最终回归和未完成项汇总。
- 临时缩放已撤销，真实游戏文件未修改，原预览服务保留。

## 续跑 7：多选复制、粘贴与删除事务（2026-09-22 08:49）

### 已复现并修复

- 浏览器先复现：返回按钮和标题同时选中，Cmd+D 只增加标题一个节点，30 → 31。
  复制副本、复制、剪切和删除现使用完整选区，按源树顺序处理；
  父子同时选中只处理最外层子树，避免重复复制或重复删除后代。
- 副本插到各自原节点之后；绝对定位副本偏移 16，使用已有移动约束逻辑，
  保留百分比/双边定位声明；流布局副本保持流布局，不强行增加像素偏移。
- 复制得到独立的内部剪贴板快照；粘贴支持整批节点，并保留旧单节点剪贴板兼容。
  复制/粘贴的新节点名称沿用 `_copy` 命名，若与活动源树或同批副本冲突则增加序号。
  不修改原节点运行时名称，不尝试重写游戏 Lua、回调字符串或引用关系。
- 锁定节点、锁定祖先、根节点或已脱离源树的对象不会导致部分操作；
  删除/剪切祖先时也检查子树锁定，保护未直接选中的锁定后代。
  剪切被拒绝时保留旧剪贴板，锁定粘贴目标不产生历史。
- 删除工具栏与快捷键面向完整选区，确认框显示实际顶层目标数及名称；
  行内删除仍仅作用该行。确认保存源树身份及目标集合，
  不会因后续选区变化或文档切换删错对象；执行时再次检查锁定状态。
- 每批变更只产生一条历史。浏览器进一步复现撤销后只恢复主节点选择，
  为历史增加可选的完整选区快照，同时兼容旧单选快照；
  撤销/重做均恢复完整选区，继续操作无需重新多选。

### 验证证据

- `tests/tree-commands.test.js` 先复现只复制主节点，再验证：
  源树顺序、父子去重、独立剪贴板、重复名称、流布局/百分比保留、
  批量删除/剪切、锁定选区和后代保护、单次快照及锁定粘贴目标。
- 新增 `tests/selection-session.test.js`，运行真实 preview/history/commands/hotkeys，
  使用内存文档及隔离 UI 外壳。先复现确认框只描述主节点、撤销丢失多选，
  再验证删除取消、冻结目标、过期确认、批量删除撤销/重做与 clean baseline、
  完整选区恢复、实际快捷键到批量复制/粘贴/剪切命令的连接。
- 浏览器：返回按钮与标题复制后 30 → 32，同时选中两个副本；
  一次撤销回到 30、恢复两个原节点的选区且 dirty 消失；
  重做回到 32 并选中两个副本，再次撤销恢复。
- 浏览器：设置面板及其中标题同时选中，复制后 30 → 56，只选中面板副本；
  一次撤销回到 30 并恢复面板和标题两个选中对象，无 dirty。
- 浏览器：两节点删除确认展示两个名称，Escape 取消后仍为 30 节点、
  两个对象选中、无未保存修改。没有点击 GUI 确认删除或执行 GUI 剪切。
- 全部 JS、Python 4 项、真实项目 37 文件/392 节点/缺图 0/源数据回环，
  `git diff --check` 通过；浏览器未观察到 error/warn。

### 仍未验收 / 下一轮收束

- 浏览器工具拦截 Cmd/Ctrl+C/V 的虚拟剪贴板路径，本轮未完成真实 GUI 粘贴：
  不将快捷键事件级测试等同于系统剪贴板实测。编辑器目前仍是内部节点剪贴板，
  不支持跨标签页或与外部 Figma 互通。
- 粘贴保留每个节点的声明，并以主选中节点作为目标容器；
  跨父容器复制后的选区相对视觉位置、百分比适配不属于已验收范围。
- 静态名称避免碰撞不等于动态模板 ID/回调引用重映射，
  模板参数和运行时绑定仍需独立规范及引擎证据。
- 编组/解组/对齐、层级快捷键的锁定一致性仍未全面修复，
  不把本轮多选命令修复记成整项“复制、编组、对齐”完成。
- 原预览服务保留，临时修改全部撤销，真实游戏文件未写入。
  下一轮优先最终回归和未完成项收束，10:00 前停止续跑，不扩展新功能。

## 续跑 8：编组顺序、坐标与收束回归（2026-09-22 09:44）

### 已复现并修复

- 浏览器反向选择音乐按钮、标题再编组，组内顺序跟随点击顺序反转，
  整个组被追加到父级末尾。现在按源树顺序取节点，组放入首个选中节点的位置；
  解组也在组原来的位置展开，不把子节点追加到末尾。
- 带边框的父容器会影响编组/解组坐标原点。使用真实布局结果校正世界位置，
  回归验证编组与解组前后两个节点的完整矩形保持一致。
- 同 zIndex 节点编组时将该层级值带到组；新组名称避免与活动树已有名称冲突。
- 复用完整选区/祖先及后代锁定校验，阻止锁定对象被编组或通过解组间接改动。
  多选解组暂不支持，不再静默只处理主节点。
- 支持范围明确为同父级绝对定位节点编组，以及单个绝对定位容器的绝对定位子节点解组。
  不再把流布局直接变为绝对定位而连带改变未选中兄弟的布局；
  工具栏禁用状态和 tooltip 与命令保护一致。

### 证据与限制

- 命令测试先复现反向选择导致组内顺序反转，再验证原插入位置、
  边框坐标、共同 zIndex、名称冲突、锁定集合/后代、流布局拒绝和多选解组拒绝。
- 会话测试补齐编组、撤销恢复原选区、重做、解组及撤销恢复组选中。
- 浏览器：同样反向选择标题与音乐按钮，修复后组仍位于独白按钮之前，
  组内标题在音乐按钮之前；截图未观察到位置跳动。
  一次撤销恢复 30 个节点、两个原对象选区和干净状态。
- GUI 解组会移除容器节点，本轮未执行；解组证据仅为内存夹具测试。
- 不保证非连续选择或不同 zIndex 的编组保留与未选中对象的交错绘制关系；
  带旋转/缩放、容器透明度/裁切等效果的解组也未通过视觉等价验收。
- 绝对定位编组保留当前矩形，会把选中节点响应尺寸固化成像素，
  不等于保留原百分比适配规则；复杂约束仍属未完成项。
- 最终浏览器复查弹窗模板 548×768（118%）、资源条模板 177×62（400%），
  图片与文本可见。回到设置页 30 节点、无 dirty/模态框，未观察到 error/warn。
- 全部 JS、Python 4 项、真实项目 37 文件/392 节点/缺图 0/源数据回环、
  `git diff --check` 通过，真实游戏文件未写入。

完整交接见 `docs/final-acceptance-handoff.md`。本次夜间修复到此收束，
剩余项目明确交接，不将整张清单标记为全部通过。
已通过应用工具删除 `ui` 自动续跑，保留本地预览服务；不会继续无限扩展修改。

## 后续验收顺序

| 顺序 | 工作流 | 当前状态 | 必须验证的细节 |
|---|---|---|---|
| 1 | 保存、项目切换 | 主要路径和键盘/确认框已修复 | 隐藏 folderInput 的只读导入仍是旧链路；真实系统授权/写回尚未 GUI 验收 |
| 2 | Assets | 加载隔离/替换/取消/画布拖入已修复验收 | 系统句柄未 GUI 授权实测；Inspector 字段拖入待补；缺图可见诊断与资源导航见第 8 项 |
| 3 | 节点树 | 导航/显隐/锁定继承/同级重排/普通改父已验收 | 重命名、多选范围和其它锁定命令待补；复杂变换另行验收 |
| 4 | 画布选择与快捷键 | 数字缩放/模态隔离/全选已修复，Shift 多选已实测 | 范围多选、穿透入口、空格拖动画布中途丢失焦点 |
| 5 | 移动、缩放与约束 | 普通移动/绝对定位缩放已实测，手势取消已有事件回归 | Flex resize、百分比/双边约束、复杂多选缩放与真实系统中断仍待专项验收 |
| 6 | 复制、编组、对齐 | 多选复制/删除事务及普通绝对定位编组已修复；解组有自动测试 | GUI 剪贴板/解组、跨父粘贴布局、动态 ID 引用、复杂编组、对齐及其它命令锁定一致性仍待验收 |
| 7 | Inspector | 常规输入及多选公共属性已验收 | 其余属性混合值、颜色/单位校验、约束批量编辑、默认值与事务边界 |
| 8 | UI/Assets 信息架构 | 待验收 | 文件搜索、目录点击/展开、选择状态、资源数量、缺省/错误/加载状态；只补实际高频缺口 |
| 9 | 预览与引擎一致性 | 仍有明确限制 | 字体单位与主题默认值、变换、自绘、模板样例数据；有引擎证据再改，不能猜着修改游戏数据 |
| 10 | 最终回归 | 本次自动回归与真实模板复查已完成；未通过项已交接 | 多窗口尺寸、系统授权及引擎像素对照仍未完成，不能据此宣布产品全面验收通过 |

## 每轮操作规范

1. 先读本清单和最近修改，查看 git status；不要从头重复调查，也不要覆盖已有修复。
2. 选择最高优先级的未完成流程，记录复现步骤或失败测试，再修改。
3. 以现有模块边界为基础，避免为“像 Figma”引入与游戏 UI 编辑无关的设计工具功能。
4. 测试跟风险匹配；数据保护和跨模块动作不能只用截图验收。
5. 浏览器风险操作需要确认时，不擅自执行；可以使用可丢弃夹具的非 UI 测试补证据。
6. 不把日志更新当作工作完成；记录实际改动、测试输出、浏览器观察和未验证范围。
7. 每轮结束保持预览可运行，把临时编辑撤销，更新本清单对应行。
8. 完成清单或到达截止时间后，总结并暂停续跑，不无限扩展范围。
